document.addEventListener("DOMContentLoaded", function clearLocalStorage() {
    // Clear all items from local storage
    localStorage.clear();
    console.log("Local storage has been cleared.");
});